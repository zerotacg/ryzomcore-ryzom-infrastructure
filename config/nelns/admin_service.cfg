// link the common configuration file
RootConfigFilename = "common.cfg";

// Im the AES, Ill not connect to myself!
DontUseAES = 1;

// I dont need a connection to a naming service
DontUseNS = 1;

WebPort = 49995;

DatabaseHost = "db";
DatabaseLogin = "ryzom";
DatabasePassword = "ryzom";
DatabaseName = "ryzom";

// can have more than one email, using { "a", "b" }
AdminEmail = "lecroart@nevrax.com";
DefaultEmailFrom = "noreply@nevrax.com";
SMTPServer = "gw.nevrax.com";

// time while email are accumulated in second (to avoid spamming)
// set -1 to dont send alert
AdminAlertAccumlationTime = -1;

RRDToolPath = "c:\bin\rrdtool.exe";
RRDVarPath = "c:\temp";

SysLogPath = "logger";
SysLogParams = "-p user.crit '[ftmms][5][1][Ryzom]: %s'";

// how many second before aborting the request if not finished
RequestTimeout = 5;

NegFiltersDebug    += { "LAUNCH", "MYSQL" };
NegFiltersInfo     += { "LAUNCH", "MYSQL" };
NegFiltersWarning  += { };
